fileapi.net: Puppet configuration files
=======================================

To setup a server, run `papply`.

Before making changes, ensure a Git pre-commit hook is setup: `ln -s ../../plint .git/hooks/pre-commit`
